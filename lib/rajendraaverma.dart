import 'package:flutter/material.dart';

class RajendraAVerma extends StatelessWidget {
  const RajendraAVerma({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(),
        body: Container(
          color: Colors.yellow,
        ),
      ),
    );
  }
}
